/*
var mongoose = require('mongoose');


var counterSchema = mongoose.Schema({
    id: Number
})

counterSchema.methods.increment = function() {
    this.id = this.id++;
    return this.id;
};

var Userid_AI = mongoose.model('Userid_AI', counterSchema);
module.exports = Userid_AI;
*/